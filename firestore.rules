rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // فتح الصلاحيات بالكامل لجميع العمليات بما في ذلك الحذف لضمان عمل كافة ميزات الإدارة
    match /{document=**} {
      allow read, create, update, delete: if true;
    }
  }
}