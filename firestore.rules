rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // فتح كامل الصلاحيات لجميع المجموعات لضمان عمل الحذف والإضافة دون قيود أمنية أثناء التطوير
    match /{document=**} {
      allow read, write: if true;
    }
  }
}